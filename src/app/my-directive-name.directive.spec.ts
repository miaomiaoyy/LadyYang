import { MyDirectiveNameDirective } from './my-directive-name.directive';

describe('MyDirectiveNameDirective', () => {
  it('should create an instance', () => {
    const directive = new MyDirectiveNameDirective();
    expect(directive).toBeTruthy();
  });
});
